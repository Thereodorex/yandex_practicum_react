import {

} from '../actions/ingredientDetails';

const initialState = {
};

export const ingredientDetailsReducer = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};