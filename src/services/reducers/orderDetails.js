import {

} from '../actions/orderDetails';

const initialState = {
};

export const orderDetailsReducer = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};